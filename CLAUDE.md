# CLAUDE.md

本文件是 Claude Code 在本仓库工作的入口规则。`AGENTS.md` 是面向所有 AI/Codex 的全局规则，**本文件与其一致，冲突时以 `AGENTS.md` 与 `docs/` 阶段需求文档为准**。

## 项目简介

`ai-cs-platform`：基于 **FastAPI + LangChain + LangGraph + PostgreSQL + Redis** 的 AI 客服聊天系统。当前处于早期阶段，按 Stage 分阶段推进，不要跨阶段超范围实现。

## 必读顺序（写代码前）

1. `AGENTS.md`（全局规则与禁止事项）
2. `docs/00_docs_management_standard.md`、`docs/README.md`
3. `docs/architecture/roadmap.md`（总体路线图 Stage 01-09）、`docs/architecture/system_overview.md`（分层架构与主链路）、`docs/architecture/implementation_summary.md`（**给人看的实现总结**——各能力实现思路横向叙述，向人讲解/新人上手首选）
4. `docs/chat/intent_taxonomy.md`（意图体系规范——意图码/优先级/风险等级的**单一事实来源**，改意图必先改它）
5. 本次任务对应的阶段需求文档 `docs/requirements/stage-xx-*/`

## 阶段进度

- **Stage 01 基础框架** —— ✅ 已实现（FastAPI / 配置 / 日志 / 异常 / 统一响应 / PG async / Redis async / Health API / Alembic 基础）
- **Stage 02 聊天核心表** —— ✅ 已实现（`chat_session` / `chat_message` / `chat_dialog_state` / `chat_decision_log`，`app/models/` Models、`app/repositories/` Repository、Alembic migration；JSONB 字段、`tenant_id`、复合索引、dialog_state 乐观锁 version）
- **Stage 03 聊天主链路** —— ✅ 已实现，2026-07-02 完成 v2 修订（确认门闭环 META.CONFIRM/DENY、ORDER.CANCEL、会话归属校验、JSONB 变更追踪、并发冲突 409、失败轮次独立事务留痕；修订清单见 stage-03 文档附录）
- **Stage 04 LLM 接入** —— ✅ 已实现（2026-07-02）：**04-02** SetFit 语义分类（三层混合：规则控制层→SetFit 29 类→降级规则，`INTENT_CLASSIFIER=rule|hybrid`，训练数据 `docs/intent/intent_train_v42_project.csv`（v41 只读原始经 build 脚本生成），test acc 0.94，**训练操作文档 `docs/intent/setfit_training.md`**——数据来源/命令参数/acc≥0.90 门禁/重训闭环含 KNN 索引重建）；**04-01** LLM Provider 工厂（`app/chat/llm/factory.py`，timeout/降级统一收口）、LLM 难例二判（SetFit 低置信才调用，意图目录从 `catalog.py` 程序化生成）、LLM 槽位兜底（规则优先只补空缺）、LLM 回复润色（仅 DONE/FALLBACK，数字事实校验违规回退底稿；补槽/确认门保持模板）、会话创建 `POST /api/chat/sessions` 与历史分页 API、意图评估门禁（`tests/eval/`）。**所有 LLM 路径在 OPENAI_API_KEY 为空时自动降级**。db_session 已迁出 GraphState（config 注入，state 可序列化）。遗留：流式 SSE、真实 LLM 端点联调
- **Stage 05 工具层与确认门闭环** —— ✅ 已实现（2026-07-02）：Skill Loader（31 个技能 md 能力声明双源合并进注册表 + 启动校验，`SKILL_LOADER_STRICT`）；ToolProvider 协议 + MockToolProvider（确定性假数据，`TOOL_PROVIDER=mock`）；**ActionExecutor 唯一写入口**（三重校验+防重放+独立事务 EXECUTING 标记，确认→执行→工单号回执）；ConfirmationResponseParser（含糊应答 LLM 解析 CONFIRM/DENY/MODIFY/UNRELATED，无 Key 降级规则）；chat_task/chat_tool_call 审计表（save_turn 统一同步任务行）；任务挂起/恢复（task_stack 启用 + 上下文槽位继承 + 自动续办提示）；tool_invoke 节点（订单/物流查询返回 mock 事实数据，失败按 R4 走 rag_fallback）。图为 8 线性节点 + 5 回复分支
- **Stage 06 RAG/FAQ/向量库** —— ✅ 已实现（2026-07-02，提前于 04/05 落地）：向量库用 **Milvus**（`VectorStoreBackend` 抽象，pgvector/ES 为后续可选后端）；PG 为知识库唯一事实来源（kb_document/kb_chunk/faq_entry，embedding 存 JSONB）；FAQ 精确层 + 混合检索（Milvus 向量 + jieba 关键词 RRF）；拒答不编造、检索轨迹落 decision_log.retrieval_json；`rag_answer` 节点条件路由接入主链路（FAQ.GENERAL 意图 / UNKNOWN 兜底）；LLM 生成路径已预留（无 Key 时 FAQ 标准答案 + 摘录式回答离线运行）；`python -m app.kb.reindex` 从 PG 重建索引
- **Stage 06-02 文档解析管道** —— ✅ 已实现：解析器链（MinerU HTTP → Docling → 内置）支持 pdf/docx/xlsx/md/txt，统一 Block IR；**结构感知切分**（标题路径注入每个 chunk、表格独立成块+大表分片重复表头、图片 caption+邻近正文合块、碎片合并）；`POST /api/kb/documents/upload` 文件入库
- **Stage 06-03 检索路由与商品库** —— ✅ 已实现：检索路由矩阵 R1-R5（按意图×对话状态路由，补槽/确认门轮次不检索）；`ProductProvider` 协议 + 本地商品表（product_item）；`product_answer` 节点：商品意图商品库优先（**价格/库存禁走 RAG**，红线）、多命中列候选、无命中宁缺勿编、ASK_INFO 叠加 RAG 增强
- **Stage 06-04 检索管道 v2** —— ✅ 已实现（2026-07-03）：query 归一化（`app/kb/query_normalize.py`：繁简转换/同义词扩展只进关键词路/型号识别→precise|semantic 判型）；多路宽召回（`RAG_RECALL_TOP_K`）+ **动态加权 RRF**（精确查询关键词路提权——型号/单号向量不可靠）；可选 **CrossEncoder 重排**（`RERANKER_PROVIDER=off|local`，失败降级）；**父子分块**（kb_chunk.section_path，命中子块聚合章节兄弟块为 section_context，行级去重）；歧义检测（top1/top2 分差小且异文档→摘录附提示）。多意图切分器评估集 `tests/eval/test_multi_intent_eval.py`（12/13）；分类器训练数据结论：分段即单意图，无需改动（intent README 4.5 节）
- **Stage 07 人工接管与工单** —— ✅ 已实现（2026-07-03）：`chat_handoff_ticket` 工单表（部分唯一索引承载「同会话一张未关闭工单」幂等，context_json 上下文移交包：任务栈/槽位/最近消息脱敏快照）；**bot 静默修复**（`chat_session.status=handoff` 时 load_session_state 意图分类前短路，回复固定等待话术，消息照常落库 status=HANDOFF_SILENT；guardrail_check 对静默轮次透传防覆盖）；五类触发收口（save_turn：USER_REQUEST / PAYMENT_ISSUE / REPEATED_UNKNOWN——连击计数存 context_stacks_json、阈值 `HANDOFF_UNKNOWN_STREAK`；action_execute：EXECUTION_FAILED；tool_invoke：SKILL_RULE 按技能 requires_human_if 声明），建单轮决策日志记录 reason+ticket_id；坐席 API `app/api/routes/handoff.py`（admin scope：队列分页/详情含 context/claim 条件更新防并发抢单 409/reply 写 role=agent 消息/resolve 归还会话+状态机复位 IDLE）。遗留：坐席实时推送、CLOSED 状态 API
- **Stage 08 鉴权与多租户加固** —— ✅ 已实现（2026-07-02，生产门槛达成）：API Key per tenant（`Authorization: Bearer ak_xxx.sk_yyy`，bcrypt 哈希存储 + TTL 缓存，密钥 CLI `scripts/manage_api_keys.py`）；scope 分离（chat/admin）；`AUTH_ENABLED=true` 时 tenant_id 一律凭证解析（请求体忽略）、session 强制服务端发号；限流（租户+会话级滑动窗口，429+Retry-After，Redis 故障放行）；`Idempotency-Key` 幂等；trace 中间件化（X-Trace-Id）；请求体限长 413。开发模式（默认）零回归
- **Stage 09 可观测与评估平台** —— ✅ 已实现（2026-07-03）：Prometheus 指标 9 个（`app/core/metrics.py` 单一注册点 + `GET /metrics`；多租户不进 label，直方图用意图域控基数）；质量看板（`quality_daily` 物化视图 + `docs/ops/quality_queries.md` 8 组可执行 SQL + `scripts/refresh_quality_views.py`）；数据回流（`scripts/export_review_set.py` review/faq 两模式：低置信/LLM 二判/FALLBACK/差评轮次导出→人工审核→`build_intent_dataset.py --extra` 合并重训；去重/脱敏/排除已入训练集，`data/export/` 不进 git）；`chat_feedback` 表 + `POST /api/chat/sessions/{id}/feedback`（会话+消息双归属校验防投毒，重复评价幂等更新）；CI 门禁 `.github/workflows/ci.yml`（ruff/mypy/pytest -rs，eval skip 显式可见）；决策回放 `scripts/replay_trace.py --trace|--session`。**同期散件**：流式 SSE `POST .../messages/stream`（meta/delta/done/error 事件，协议见 chat_api.md 第 7 节）；RAG 评估集 32 组（`docs/testing/rag_eval_set.md` 用例表 + `tests/eval/test_rag_eval.py` harness 解析执行，阈值双档 hash 0.75/0.80 实测通过）；`docker-compose.yml`（PG+Redis 默认，`--profile kb` 加 Milvus standalone）。遗留：Grafana 看板、SetFit CI 门禁需带模型 runner、RAG 高档门禁待真实 embedding
- **Stage 11 MCP 工具服务与集成** —— ✅ 已实现（2026-07-03）：订单/物流查询以 **MCP 标准工具服务**提供（`scripts/run_mcp_server.py`，FastMCP streamable-http :8400/mcp，数据源与进程内 mock 共享 `mock_data.py` 可对拍）；`McpToolProvider`（`TOOL_PROVIDER=mcp`）：list_tools 动态发现（失败缓存带 60s TTL 自动恢复接管）、服务未覆盖工具（写操作）回落 mock、调用失败/服务不可达回落 mock 并打 `degraded` 标记；聊天链路与审计表零改动（Stage 05 抽象层验证）。对接真实业务系统只需替换 MCP 服务端工具函数内部
- **Stage 12 Langfuse 链路追踪** —— ✅ 已实现（2026-07-03）：`app/core/tracing.py` 工厂+降级；LangChain CallbackHandler 挂 LangGraph run config（节点 span + LLM prompt/token 自动捕获，节点代码零改动）+ `chat_completion` 收口；trace 关联 session/user/tenant/业务 trace_id；`LANGFUSE_ENABLED/PUBLIC_KEY/SECRET_KEY/HOST` 配置，**无 Key/SDK 失败/服务不可达一律静默降级**；lifespan 关停 flush。与 Prometheus 互补：指标管聚合告警，Langfuse 管单次调用链路明细
- **Stage 13 生产加固** —— ✅ 已实现（2026-07-03，审计整改三批全部完成）：生产配置硬门禁（`APP_ENV=prod` 时 AUTH/DEBUG/hash embedding/弱口令缺项**拒绝启动**）；**管理面强制 token**（开发模式空 `KB_ADMIN_TOKEN` 不再放行，联调需在 .env 配置）；ActionExecutor 防重放原子化（`claim_for_execution` 条件 UPDATE，并发确认恰好执行一次，无 task_id 拒绝）；MCP 失败禁 mock 冒充事实（`TOOL_MCP_FALLBACK=fail` 默认，UPSTREAM_UNAVAILABLE 走既有失败分支；写操作恒走 mock）；幂等加固（显式 commit 后写缓存 + SET NX 在途锁 409 + body 指纹 422）；API Key 即时吊销（Redis 版本广播 + LRU 缓存 + 假 bcrypt 拉平耗时 + expires_at）；Prometheus 多进程（`PROMETHEUS_MULTIPROC_DIR`）；L3 弱确认收紧（「好的」→「请回复『确认』」重确认）；脱敏扩展（地址打码 + decision_log 落库脱敏）；建单 SAVEPOINT 防事务污染；FAQ needs_reindex；启动 DB 探活；SKILLS_DIR/模型路径锚定仓库根；限流先判后写。migration `504825e337b1`
- **Stage 14 内容安全护栏** —— ✅ 已实现（2026-07-04）：规则库在 `docs/chat/skills_design/guardrails.md`【机器可读规则库】表格（**单一事实来源**，改规则改表格；injection 正则/abuse_severe 词表/emotion_negative flag/output_leak 四类），`app/chat/guardrail/`（lexicon 解析 + engine 引擎，规则损坏/Redis 故障一律 fail-open）；`guardrail_check` 实装：注入拦截固定话术、重度违禁连击 `GUARDRAIL_ABUSE_STREAK=2` 次→ABUSE 工单+会话静默（计数走 Redis 不触碰 dialog_state——拦截轮不动状态机的 v2 语义）、轻度情绪 flag→润色 soften、同文本连发 `GUARDRAIL_REPEAT_LIMIT=3` 次拦截；`app/chat/llm/prompt_guard.py::wrap_user_input()` 防注入收口全部 5 个 LLM 拼接点（二判/槽位/润色/确认门解析/RAG 生成/记忆抽取）+ RAG system 硬约束；输出护栏 `check_output`（润色违规回退底稿、RAG 弃生成走摘录、记忆摘要/事实写入前过滤）；`guardrail_blocks_total{rule}` 指标 + 决策日志 graph_trace_json.guardrail 留痕；训练语料 500 条零误拦扫描做回归防线。遗留：GUARDRAIL_PROVIDER=external 扩展位、词表运营扩充
- **Stage 15 体验闭环** —— ✅ 已实现（2026-07-04）：**WS 双端实时**（`app/api/routes/ws.py` + `app/services/notify_service.py` WsHub——用户端 `WS /api/chat/sessions/{id}/ws` 收 agent_reply/session_resumed/proactive，坐席端 `WS /api/handoff/ws` 收 ticket_created/user_message；Redis Pub/Sub 跨进程广播、故障回落本进程直投、断线即弃重连拉历史；鉴权 verify_bearer_token 共用，开发模式 query 参数）；**CSAT**（chat_csat 表 + `app/chat/csat.py` 严格解析防误吞；resolve/会话关闭发询问置 csat_pending，评分回复 load_session_state 短路捕获→save_turn 落库清标记，非评分一次性失效；quality_daily 加 csat_avg/csat_count，低分≤2 进回流导出）；**会话生命周期**（`scripts/close_idle_sessions.py` cron：空闲关闭+CSAT 询问、ASSIGNED 超时 CLOSED+会话归还——补 Stage 07 遗留；closed 会话来消息自动重开、任务不复活）；排队位置反馈（建单回复附「排队第 N 位」）；主动消息 `POST /api/chat/sessions/{id}/notify`（admin scope）。migration `b546c33dea6f`。遗留：坐席工作台前端、WS 心跳/连接数走网关层。更远 backlog（多渠道/知识库后台/多模态/多语言/成本控制/A B 实验）见 roadmap 3.6
- **Stage 16 知识库运营后台** —— ✅ 已实现（2026-07-05）：`kb_document` 加 `published_version`/`effective_from`/`expire_at`；`kb_document_version` 版本表 + `kb_operations_service`（草稿-审核-发布状态机 draft/pending_review/published/archived）；**生效判据 = published_version 非空且未 archived**（不单看 status——编辑已发布文档 status 回 draft 但 published_version 不变，线上仍服务旧版本，直到再发布）；版本回滚、审核流（动作落 metadata_json.review_log）、发布复用 `upsert_document` 重建索引；`scripts/kb_schedule.py` 定时生效/失效 cron；`docs/ops/kb_quality_queries.md` 命中率/盲区 SQL（decision_log 聚合）；`routes/kb.py` 扩 8 个运营 API（admin scope）。migration `3a1e7630141b`。219 tests 零回归（基准 213+6）+ 真实 Milvus e2e。遗留：运营前端、角色细分（预留 kb_editor/kb_reviewer）
- **Stage 17 LLM 成本控制** —— 📝 需求已写：`docs/requirements/stage-17-llm-cost-control/`——语义缓存（同问直答，事实类禁缓存红线，不依赖真实 LLM）、租户 token 预算与熔断（超限降级模板）、模型分级路由（简单轮走小模型）
- **Stage 18 A/B 实验框架** —— 📝 需求已写：`docs/requirements/stage-18-ab-experiments/`——确定性 hash 分桶、变体只覆盖已有可配项、decision_log 落变体、按变体切分 quality_daily 对比（框架可落地，显著性结论待真实流量）
- **Stage 19 多语言地基（i18n）** —— ✅ 已实现（2026-07-04）：`app/core/i18n.py`（`t(key, locale, **params)`/`skill_template`/`resolve_locale`，缺 key 回退默认语言）+ `app/locales/`（zh 源逐字对齐现有文案=**零回归**、en 骨架示范）；面向用户确定性文案全部收口 `t()`（responder/guardrail/response_generate/save_turn/action_execute/product_answer/handoff/close_idle）；skill 模板语言覆盖（registry 中文=zh 源不动，非默认语言查 `skill.<id>.<key>` 覆盖）；LLM 提示词加「回复语言与用户消息一致」（**不翻译提示词**，模型自答）；locale 贯穿（请求 `ChatMessageRequest.locale`＞会话 metadata 记忆＞`LOCALE_DEFAULT`，`load_session_state` 决策并写入 metadata 供后续轮沿用）。全量 213 tests 零回归 + e2e（en/zh 护栏话术、locale 记忆）验证。做语言=加 `app/locales/xx.py`+skill md `templates.xx`。遗留：输入理解侧（意图分类多语言训练集、护栏词表/槽位正则）、skill.name 多语言、自动语言检测
- **Stage 10 多意图/任务治理/记忆** —— ✅ 已实现（2026-07-03，2026-07-15 事务一致性修订）：**多意图**（`app/chat/intent/multi_intent.py`：并列标记分段+每段独立分类抽槽防串槽，次要意图入任务栈自动续办；恢复即完成的读任务提示「回复继续」触发执行）；**任务治理**（`TASK_TTL_MINUTES` 过期任务不复活、`TASK_MAX_ASKS` 追问超限放弃+转人工建议）；**记忆系统**（`app/chat/memory/`：MemoryProvider 协议，`MEMORY_PROVIDER=local|mem0|off`——local=短期窗口+LLM 会话摘要（存 chat_session.metadata_json）+长期事实（user_memory 表）；mem0=长期记忆托管 mem0（Milvus collection），无 Key/未装包自动降级 local；注入 LLM 润色与 RAG 生成；记忆写入改为主事务 after-commit 后异步执行，rollback 不产生幽灵记忆，关停时收敛后台任务）。修复：chat_message.created_at 改 clock_timestamp()（同事务消息排序稳定）
- **Stage 17 LLM 成本控制** —— ✅ 已实现（2026-07-04）：三块可独立开关、独立降级、**默认全关=零回归**。**分级路由**（`factory._model_for_purpose`：classify 走 `CHAT_MODEL_FAST`、generate 走 `CHAT_MODEL_SMART`，留空逐级回落 `CHAT_MODEL`；RAG 生成走 smart）；**租户 token 预算熔断**（`app/chat/llm/budget.py`：租户 contextvar 入口注入 + Redis 按日 INCRBY 累计 + `is_over_budget`，达日预算后 `chat_completion` 与 RAG 生成双收口降级模板——与无 Key 同路径；usage 从 provider `usage_metadata` 取，`account_llm_result` 计指标+累计）；**语义缓存**（`app/chat/cache/semantic_cache.py`：`SemanticCacheProvider` 协议 + Redis 每租户封顶列表 + Python cosine，复用 `embedding_client`；只缓存无副作用来源白名单 `faq/rag_llm/rag_extract/chitchat`——**价格/库存/订单事实永不缓存**；接入 `rag_answer` 命中短路，KB publish 后按租户失效；后端故障 fail-open）。指标：`llm_tokens_total`/`llm_budget_exceeded_total`/`semantic_cache_total`。遗留：真实 embedding 泛化标定阈值、闲聊节点接入、月度预算窗口
- **Stage 18 A/B 实验框架** —— ✅ 已实现（2026-07-04）：`app/experiments/`（config JSON 配置按 mtime 缓存 + resolver 确定性分桶）；**分桶** `md5(exp:tenant:session)%100`（不用内置 hash 防 PYTHONHASHSEED 随机化，多进程一致、同会话稳定同变体）；**白名单** `OVERRIDABLE_PARAMS`（仅 RAG_MIN_SCORE/FAQ_HIT_THRESHOLD/RAG_RECALL_TOP_K/RERANKER_PROVIDER，配置解析层过滤非白名单键——实验只调参数不引新代码分支）；**参数注入** `set_overrides` 写 contextvar → 消费点经 `effective(name)` 读（覆盖优先否则回落 settings，改了 kb/retriever·answerer·rerank 4 处）；图入口 `chat_service` 收口 `resolve_experiment`→注入+落 `chat_decision_log.experiment_json`（migration `a7c2f1e9d3b4`）；对比 SQL `docs/ops/experiment_queries.md`（按变体切 done/handoff/refused/P95/CSAT+两比例 z 近似，**显著性结论留运营**）；CLI `scripts/experiments.py list|bucket|sample`。配置缺失/损坏/作用域外/stopped 一律 fail-open 走 control。遗留：意图域作用域（入口先于分类，v1 仅记录）、配置改 DB+前端
- **Stage 21 智能澄清** —— ✅ 已实现（2026-07-27）：UNKNOWN+FALLBACK 轮次用 LLM 生成针对性澄清问句（`app/chat/skills/llm_clarifier.py`：top_k 业务候选过滤 + catalog 描述程序化生成 + 近期对话注入 + 防注入/输出治理/护栏），替代固定模板；接入 `response_generate` 与 `rag_answer` 拒答回落两出口（graph_trace 记 `:clarify`，指标口径不变）；无 Key/失败降级模板零回归；unknown_streak 转人工安全网不动。**单次 LLM 调用非 agent 循环**（三层演进第一层，见 post-stage-20 记录第 8 节）。`CLARIFY_LLM_ENABLED`。遗留：澄清成功率 SQL 口径、真实 LLM 问句质量评估
- **Stage 22 只读诊断 agent** —— ✅ 已实现（2026-07-27）：解释性查询（"为什么还没到"）的多步只读调查（`app/chat/agents/diagnose.py`），ReAct 受约束变体——**工具箱只有 query_* 白名单（写永远走确认门）、终止全部结构性（步数上限/解析失败/重复调用/连续失败，不解析模型输出决定停）、完全可降级**；解释段数字事实校验（不在观察数据中的数字整段丢弃）+ 输出护栏；tool_invoke 静态链成功后按启发式触发，解释追加事实底稿（底稿不经改写红线不动）；审计照落 chat_tool_call，`diagnose_agent_total` 指标。**默认 `DIAGNOSE_AGENT_ENABLED=false`（零回归），联调开启**。三层演进第二层。遗留：真实 LLM 质量评估、MCP 新读工具同步白名单
- **Stage 23 对话方向纠偏** —— ✅ 已实现（2026-07-27）：错方向三盲区各补一条**纯规则**防线（零 LLM 依赖）——**任务中途否定**（COLLECTING 下「不是要退货/不对」判 META.DENY `RULE_TASK_DENY`，残差判定防吞新诉求、含数字不判；状态机仅终止当前任务栈照常恢复，回复重定向话术）；**零进度恢复降调**（误开挂起任务不再强制续问，给退出话术与否定通道闭环）；**中置信软确认**（SETFIT/LLM 来源 <0.60 新开任务追问前复述意图，不阻塞不加轮次）；`direction_correction_total` 指标 + quality_queries 第 9 组错向监控 SQL。遗留：quality_daily 扩列待真实流量
- **Stage 24 部署编排**（规划 B1）—— ✅ 已实现（2026-07-27）：`Dockerfile`（uv 多阶段/非 root/依赖层缓存，models 不进镜像缺失自动降级）+ `docker-compose.prod.yml`（api 多 worker+多进程指标 tmpfs+ready 探针 / cron / migrate / mcp·kb profile；口令零明文 `${POSTGRES_PASSWORD:?}` 强制插值）+ `deploy/scheduler.py`（纯 stdlib 三任务调度：间隔 env 可调 0=停用、失败重试线性退避、最终失败 `ALERT_WEBHOOK_URL` 告警、单任务失败隔离）+ `docs/ops/deployment.md`（部署清单/探针口径/备份/K8s 映射）。遗留：镜像构建走 CI、B2 告警覆盖容器挂掉
- **Stage 25 监控告警**（规划 B2）—— ✅ 已实现（2026-07-27）：**指标基数整改**（llm_tokens/budget_exceeded 去 tenant label——多租户不进 label 原则回归，租户明细走 Redis 日计数+quality_daily，测试防回潮）；`deploy/monitoring/`（Prometheus 抓取+8 条告警规则、Grafana provisioning+10 面板看板 JSON、alertmanager example）；compose `monitoring` profile（密码强制插值）；cron 心跳 healthcheck（调度器每 tick 写心跳，20min 时效+restart 自愈，收口 Stage 24 遗留）；告警/看板引用的指标名与 metrics.py 静态交叉校验防漂移；`docs/ops/monitoring.md`（面板/规则/演练/租户明细口径）。遗留：阈值 B3 标定、演练在部署环境执行
- **Stage 26 意图决策加固** —— ✅ 已实现（2026-07-30，需求 `docs/requirements/stage-26-intent-decision-hardening/`）：任务进行态决策四缺口修复，零 migration 零协议变更。**P1 补槽守护**（`app/chat/slots/pending_fill.py`：COLLECTING 下 pending 槽位定向提取——「订单号是12345678」不再进 SetFit 全表误开新任务；证据分级 explicit_slot_name/pure_value/contextual_answer + 四条防误填规则（类型冲突/字段名冲突/量词金额语境/残句判定）；接入 `classify_control` 排全部控制语义之后（顺序红线），rule/hybrid/multi_intent 三方共享；命中出 META.SLOT_ONLY `RULE_PENDING_SLOT`，slot_extract 直接采信定向值防同轮污染；多意图「补槽段+业务段」主意图=续接、业务段入栈）；**P2 margin 路由**（top1-top2 < `INTENT_MIN_MARGIN` 视为难例→LLM 二判，二判不可用采纳 top1 打 `SETFIT_LOW_MARGIN` 交软确认接住——**绝不改选 top2**；margin 落 intent_result 供离线标定）；**P3 切换守护**（补槽/确认门中新意图须状态分级高置信 `INTENT_SWITCH_THRESHOLD_COLLECTING/CONFIRMING` + margin 达标或显式切换信号（「另外/顺便」）才挂起切换，证据不足保留任务回复二选一澄清；UNKNOWN+有任务续接收紧为只并入缺失槽位的值（防「12345678个问题」污染 order_id，CONFIRMING 防已确认值被覆盖），无证据不填槽不切换改二选一澄清）；**P4 软确认 quirk 修复**（写意图单列 `INTENT_SOFT_CONFIRM_THRESHOLD_WRITE=0.75` 修死区；LLM 二判来源一律复述修固定置信 0.7 死条件）。指标沿用 `direction_correction_total{kind=switch_guard|unknown_hold}`。57 项专项回归 `tests/stage26/`，全量零回归。遗留：全部阈值待真实流量标定、混淆对级 margin。**遗留 3 示例向量交叉验证已实现（2026-07-30，默认关）**：`example_knn.py` margin 小难例查训练集近邻（SetFit 同源语义体），同意 top1 免 LLM 二判采纳 `SETFIT_KNN_CONFIRMED`、不同意只附证据绝不改选；索引 `scripts/build_intent_example_index.py`（SetFit 重训后必须重建）；`INTENT_EXAMPLE_KNN_ENABLED=false`，**启用时机清单与 LTR 后续路线见 docs/intent/README 第 6 节**（真实流量+二判成本可见后开启）。**低置信闲聊救援**（2026-08-05，随同一开关）：家常开放域表达 SetFit 低置信高频——top1 是 CHITCHAT.\* 且近邻同意、相似度 ≥`INTENT_KNN_CHITCHAT_MIN_SIM`(0.70) → 免二判采纳 `SETFIT_KNN_CHITCHAT`；红线：只确认 top1 不改选、只救零副作用闲聊类、**业务意图低置信永不救援**；二判目录 CHITCHAT 描述充实（家常/情绪/生活琐事）
- **Stage 27 Meta-classifier 会话决策器** —— ✅ v1 离线管线已实现（2026-07-30，需求 `docs/requirements/stage-27-meta-classifier/`，操作文档 `docs/intent/meta_classifier_training.md`）：学习目标是**对当前任务的操作决策**（续接/切换/接受新意图/送二判/澄清/未知）而非意图——即 Stage 26 手调阈值承担的决策；训练数据 `data/电商客服_MetaClassifier_合成训练数据_v1.csv`（12501 行合成表格特征，case_family 组安全 split）；`scripts/train_meta_classifier.py` 四模型对比（LR 基线/浅决策树规则发现/LightGBM/XGBoost，依赖组 `uv sync --group train`，未装自动跳过）+ 业务代价指标（误切×5/误吞×3/漏二判×2/多花LLM×1）+ 泄漏黑名单强制（`llm_review_expected` 实证为标签改写、message 文本禁用）；**部署域 = control_result==NONE 的 6 类 9002 行**（规则域运行时被 classify_control 短路到不了 Meta 层）。实跑结论：合成标签是规则生成的，LR 即 100% 拟合——**离线冠军不构成上线依据**，v1 交付物是管线+特征契约（特征与 Stage 26 落进 decision_log 的 margin/pending_fill/守护记录一一对应）。**影子模式已接入**（`app/chat/intent/meta_shadow.py`，dialog_state_resolve 内对语义层轮次预测——**只观察不决策**：落 `decision_log.graph_trace_json.meta_shadow`（decision/actual/agree/model）+ `meta_shadow_total{decision,agree}` 分歧率指标；`META_SHADOW_ENABLED=true` 默认开、产物/依赖缺失/异常一律 fail-open 静默跳过=零行为影响；实际决策对照口径见 `map_actual_decision` 注释）。**真实训练数据回流**（上线即积累）：影子每轮把特征向量+实际决策弱标签落 `graph_trace_json.meta_shadow.features/actual`——**不依赖模型产物**（采集与预测独立降级）；`scripts/export_meta_training_set.py` 导出训练契约 CSV（session md5 分桶组安全、分歧行加权 1.5 优先审核、导出列==训练白名单防漂移测试），改标后 `--data` 直接重训。契约+影子+回流测试 `tests/stage27/`（22 项）。规则系统与确认门不被替代（模型前短路+模型后兜底）。**决策融合层评审固化**（2026-08-05）：阶段 B 输出契约入文档 4.6 节（decision/final_intent/confidence/need_llm/need_clarification/evidence/reason_codes/contract_version；RAG 路由与任务恢复**不进学习层**保持结构性）；影子记录加 `reason_codes` 证据派生原因码（`derive_reason_codes` 纯函数，审核/分歧分析可 SQL 聚合）；`SETFIT_KNN_CONFIRMED` 补进影子部署域（口径修正）。**标签/评估纪律加固**（2026-08-05 评审二批）：导出三列标签（policy_decision 链路事实不可改 / reviewed_decision 人工审核专用列——维持原判也显式填写与未审核区分 / target_decision 训练标签初始=policy），训练侧 reviewed 优先 ×2 加权 + `--reviewed-only` 硬执行「未审核不进训练」；hindsight_tier 证据分级（task_deny=强、低分差评=中、仅转人工=弱——后见信号只排审核优先级永不当真值）；`--split-by time` 整会话时间切分（旧训新测防漂移虚高）；接管**十条门禁清单 + 五级分级接管顺序**（SEND_TO_LLM 先试点、SWITCH/CONTINUE 最后）固化 4.5 节；两阶段模型拆分记为备选（真实混淆矩阵后决策，不提前复杂化）。遗留：阶段 B 阈值接管（按门禁清单逐项过）、has_business_object 运行时特征、分歧率 Grafana 面板、概率校准（文档第 6 节）
- **Post-Stage 27 工程护栏**（Dify 变量纪律对照评审落地）—— ✅ 已实现（2026-08-04，需求 `docs/requirements/post-stage-27-engineering-guardrails/`）：零行为变更零 migration 零重训。**① 节点写契约**（`app/chat/graph/contracts.py`：14 个节点的写入字段按 AST 审计如实登记为 NODE_WRITES 表，builder 构建时统一包装执法——越权写 GraphState 字段 dev/test 抛 `GraphContractViolation`、staging/prod 只告警放行；functools.wraps 保签名防破坏 LangGraph config 探测；静态测试锁「注册节点==契约键集合」「契约字段⊆GraphState 声明」。节点新写字段必须同步契约表——diff 显式化职责扩大）；**② 只读工具白名单元数据化**（`app/chat/tools/catalog.py` 工具目录单一事实来源：readonly 声明推导诊断 agent 白名单（与 Stage 22 硬编码集合一致=零变更，测试锁定）+ MCP `readOnlyHint` 注解运行时合并（服务端新增读工具注册即入列，收口 Stage 22 遗留）；红线：目录声明写的工具外部怎么标只读都拒绝、alias 不进 prompt 防 LLM 打转、合并失败退回基础白名单）。评审结论同步固化：Dify 意图识别不学（每轮 LLM 分类无状态感知），变量分层五原则四条已有本次补第五条,GraphState 不做命名空间重构。tests/graph+tests/tools 16 项，全量 451 passed
- **Stage 28 Web 测试控制台（前端地基）** —— ✅ 已实现（2026-08-05，需求 `docs/requirements/stage-28-web-console/`）：`web/` 目录，**Vue 3 + Vite 4**（本机 Node 16，Vite 5 需 18+，TS 钉 ~5.4 兼容 vue-tsc 1.8）+ TypeScript + Pinia + Vue Router + Element Plus。**登录 = API Key**（与后端鉴权模型一致零后端改动：Key 留空=开发模式请求体带 tenant_id，非空走 Bearer；401/403 全局拦截清凭证回登录页）；**左侧多级折叠菜单由路由 meta 驱动**（加页面=加路由记录）+ 顶栏连接信息 + 右侧内容区；**对话控制台**（建会话/发消息/历史分页/AI 回复附 intent·status·state·trace 决策标签/测试话术速填覆盖补槽·确认门·切换守护·多意图路径）；坐席/知识库/商品挂占位页排队实化。开发经 Vite 代理 /api 零 CORS（`VITE_API_TARGET` 可换目标），生产静态托管+同域反代或 `CORS_ORIGINS`。e2e 冒烟验证（代理建会话→「我要退款」→NEEDS_SLOT/COLLECTING 标签齐全）。**WS 实时通道已接**（2026-08-05：建会话自动连用户端 WS——坐席回复/会话归还/主动消息渲染为坐席/系统气泡，状态标签+手动重连；Vite 代理 ws:true；e2e 实收 proactive 推送；已知边界：浏览器 WS 无法带 Bearer 头,鉴权模式如实降级显示不可用,未来=后端短时 WS 票据）；**部署运行文档** `docs/ops/web_console_deploy.md`（Nginx 同域反代含 WS 升级/跨域备选/故障排查表）。遗留：SSE 流式、坐席工作台实体页（Stage 15 遗留）、知识库运营页（Stage 16 遗留）、商品页、web 构建进 CI（文档第 6 节）
- **Stage 29 控制台全接口覆盖（批 1：观测查询）** —— ✅ 批 1 已实现（2026-08-05，需求 `docs/requirements/stage-29-console-coverage/` 含 25 接口覆盖矩阵）：**盘点结论**——分析类查询接口是真实缺口（决策日志仅 CLI、会话列表/KB 列表/商品列表无接口）。**批 1 落地**：`app/api/routes/observe.py` 只读 4 GET（admin scope，开发模式 X-KB-Admin-Token）——会话列表（用户/状态过滤+分页）/消息流（admin 免 user_id）/逐轮决策日志（intent_result 含 margin·pending_fill·example_knn，graph_trace 含 guardrail·meta_shadow，retrieval/experiment/latency/error 全量透出=`replay_trace.py` 页面化替代）/工具调用审计；落库已脱敏原样透出不绕过（Stage 13 纪律）；跨租户/不存在统一 404。前端「观测分析→会话记录」：会话表格+过滤分页→详情抽屉三 Tab（消息流/决策日志逐轮折叠展开 JSON/工具调用）；登录页加管理令牌字段（client 统一注入头）。协议文档 chat_api.md 第 9 节。e2e：三轮对话→RULE_PENDING_SLOT+pending_fill 证据可见、审计含 create_refund_ticket、404/403 校验。tests/stage29 4 项，全量 455 passed。**批 2 ✅**（2026-08-05）：坐席工作台实体页（队列/上下文移交包/认领/回复/解决归还 + 坐席端 WS 新单通知·用户消息实时刷新——收 Stage 15 前端遗留）+ 对话控制台 👍👎 反馈；e2e 双向 WS 闭环（agent_reply/session_resumed 实收）。**批 3 ✅**（2026-08-05）：后端补 3 个 list 接口（`GET /api/kb/documents`·`/api/kb/faqs`·`/api/product/items`，全状态+关键词过滤）；前端知识库文档页（草稿/提审/通过/驳回/下线/版本回滚/上传/直接发布——收 Stage 16 前端遗留）+FAQ 页+商品页+SSE 流式开关（delta 渐进/done 落标签）+系统状态页；e2e 三列表+SSE meta→delta→done。**覆盖矩阵闭环：32 接口全部页面可用,占位页清零**。遗留：observe 游标分页、trace 全局搜索（真实流量后）
- **Stage 30 Conversation Mode Gate（对话模式门）** —— ✅ 已实现（2026-08-05，需求 `docs/requirements/stage-30-conversation-mode-gate/`，训练文档 `docs/intent/mode_gate_training.md`）：**双轴决策系统**——模式轴（SOCIAL_ONLY/TASK_ONLY/MIXED/OOS，`app/chat/mode/gate.py`）与任务操作轴（Meta 六类）分离，**Meta-classifier 不加 CHITCHAT 类**（MODE_SOCIAL 不进影子部署域，测试锁定）；解决闲聊双 LLM 调用（先二判是不是闲聊再生成回答）。mode head **共享 SetFit body**（`predict_from_embedding` 一轮只编码一次，SetFit 重训必须重训 mode head）+ Platt 校准；**v1 只接管高置信 SOCIAL_ONLY**（组合判据：0.88 分数线+0.20 margin+业务反证拦截——强业务词/槽位值形态/切换信号，任务中加严 0.92；红线**业务信号优先于闲聊信号**），TASK/MIXED/OOS/UNCERTAIN 全部影子（证据落 `intent_result_json.mode_gate`）；**SOCIAL_HOLD**=确定性策略非训练标签（状态机 CHITCHAT-DONE 既有语义保任务 + response_generate 补续办提示）；OOS≠闲聊（能力边界回复子开关默认关，收口澄清入口省澄清 LLM）。数据 `docs/intent/intent_mode_v43_package/`（14400 行四类均衡，MIXED/OOS 合成冷启动）；训练 `scripts/train_mode_gate.py`（LR+校准，实测 acc 0.958 / SOCIAL precision 0.945）；`conversation_mode_total{mode,accepted}` 指标。**默认 `MODE_GATE_ENABLED=false` 零回归**。阶段 2（v43 业务版 25 类重训 SetFit+KNN 闲聊救援退役）待真实流量接管稳定后，清单见需求第 10 节。**评审二批加固**（2026-08-05）：**模板族泄漏实测坐实**（76/82 generation_family 跨 split，合成 test 0.958 vs 人工 hard test 0.675）——`docs/intent/mode_hard_test_v1.csv`（40 条禁用生成模板，训练脚本自动评估打印错例，**验收/阈值标定以 hard test+影子为准**）；误吞方向已闭环（「我要找你们负责人」0.934 直通实测→升级/问题类词补反证词表，危险样本全拦+干净闲聊零误伤，回归锁定）；**body 指纹契约**（mode_spec.json 落 SetFit 权重指纹，运行时不一致拒绝启用防旧头新向量静默失真）；`docs/chat/conversation_mode_taxonomy.md` 模式单一事实来源（与意图 taxonomy 双轨，UNCERTAIN=拒识非训练标签）；README 3.5 节 v42→v43 解耦章节（四类去向+切换时机红线）、KNN 闲聊救援标注为兼容降级路径（25 类后分支死代码预警）。遗留：阈值标定、MIXED 主动分段、合成数据真实化回流、hard test 持续扩充
- **Stage 31 主动服务地基（NBA + 活动引导）** —— ✅ 已实现（2026-08-05，需求 `docs/requirements/stage-31-proactive-nba/`——外部 Skill Pack `docs/ai_customer_service_skill_pack_v1/` 8 能力的取舍版：v1 只落 NBA 规则策略+活动引导+回复追加，onboarding/discovery/playbook/journey 推迟理由见需求第 2 节）：**第三条决策轴**（业务策略轴：主任务完成后是否追加至多一个主动动作；系统动作≠用户意图，不进 taxonomy/SetFit/Meta）；`app/chat/proactive/`（campaigns 配置池 mtime 缓存 + nba 规则策略）；**全局抑制矩阵**（护栏拦截/人工接管/CSAT/未完成/负面情绪/AFTERSALE·PAYMENT·CANCEL 高风险流程/SOCIAL_ONLY 轮一律禁营销，包冲突示例表全覆盖测试锁定）；活动配置驱动（`configs/campaigns.json`，eligible_intents 意图相关性防「有活动就推」，hook 运营写死**不经 LLM** 红线）；频控/拒绝闭环全 Redis 零 migration（会话上限/客户×活动上限/拒绝冷却 48h/强拒绝 opt-out，**fail-closed**——营销宁可少发不超发）；save_turn 回复定稿点收口（影子 ENABLED 只落 `graph_trace_json.proactive` 不改回复，APPLY 才追加 i18n 模板）；`proactive_actions_total{action,outcome}` 指标。**同批：Skill 声明目录迁仓库根 `skills/`**（31 个 md，运行时加载物不放 docs；schema/guardrails 规则库留 `docs/chat/skills_design/`）。**默认双关零回归**。遗留：discovery 意图新增流程、playbook 引擎、拒绝检测语义化、活动配置后台化
- **Stage 32 选品顾问与商品对比** —— ✅ 已实现（2026-08-05，需求 `docs/requirements/stage-32-product-advisor/`——skill pack stage-31/32/35 取舍版）：**盘点发现 PRODUCT.RECOMMEND（830 训练样本）/COMPARE（832）已是训练意图+注册 taxonomy+有 skill 但均为空壳**（不收槽位不查库回客套话即 DONE），本 Stage 实做为真实能力，**零新增意图零重训**。RECOMMEND：required_slots=[category, budget]（taxonomy 同步修订，collect 模板一次问齐；预算正则 千/万/k/w+以内/不超过 归一化为元、品类词表长词优先、一次性表达「预算300以内推荐个风扇」同轮抽满免追问）→ `search_by_constraints` **SQL 层硬约束**（上架+有货+品类+预算内，给预算时无价格商品也排除）→ 价格升序 top4（无商业权重输入）+ trade-off + 对比引导；COMPARE：compare_items（「A和B/A vs B」捕获剥噪声规范「A|B」）→ 逐款查库结构化并列。红线沿用+新增：候选只来自 product_item 表、不满足硬约束不进候选、无命中/故障宁缺勿编、推荐理由只引用结构化字段。**Playbook 引擎不建二套**——包验收逐条映射既有机制（任务状态机=step、task_stack=挂起恢复、TTL/MAX_ASKS=超时放弃、ActionExecutor=写恰好一次、decision_log=回放，映射表见需求 1.3 节）；购买辅助只取对比，其余推迟。默认开启（响应式读能力 P4，非主动营销——与 30/31 默认关的理由差异见需求第 3 节）。tests/stage32 22 项
- **Stage 33 会员注册引导（onboarding，mock 版）** —— ✅ 已实现（2026-08-05，需求 `docs/requirements/stage-33-member-onboarding/`——skill pack new_user_onboarding 取舍版，mock 会员工具打通整条机制链路，对接真实系统只换工具内部）：**MEMBER 域**（taxonomy 第 10 域）+ `MEMBER.REGISTER`（L2 写）——规则层确定性触发（「注册会员/开通会员/裸『注册』」，否定语境/第三方平台语境『注册抖音账号』=OOS 不劫持、CONFIRMING 确认门优先，测试锁定）+ LLM 二判目录兜底（零重训）；技能 WRITE：phone 槽位→确认门→ActionExecutor `register_member`（executor 补 user_id 系统上下文参数与幂等键同级）→回执含确定性会员号；mock 工具 `query_member_status`（readonly 进诊断白名单，目录锁测试同步）/`register_member`（进程内 MEMBERS 闭环：注册后再查已注册）；**NBA START_ONBOARDING**（包 stage-33 低风险动作第一批，活动之前获客优先）：未注册确认才建议（查询失败/无 user_id fail-closed 不骚扰）+共享抑制矩阵/频控/冷却+客户级上限 `PROACTIVE_ONBOARDING_MAX`，话术引导显式回复「注册」（确定性入口，不做弱确认解析）；受主动服务总开关双关管控=默认不生效，用户主动路径独立可用。技能 md 31→32。遗留：验证码环节（需任务中途工具机制，随真实会员系统）、MCP 服务端 member 工具、旅程阶段起点。tests/stage33 21 项
- **Stage 34 Case 工单与 SLA 治理（含 Service Recovery）** —— ✅ 已实现（2026-08-05，需求 `docs/requirements/stage-34-case-sla/`——roadmap 3.7 服务闭环第一项）：**Case ≠ Task**——跨会话的客户问题服务记录（`chat_service_case` 表 + migration `ae19ae99e128`，可回滚实测）。9 态生命周期（OPEN/IN_PROGRESS/WAITING_×3/RESOLVED/CLOSED/REOPENED/ESCALATED，白名单迁移表非法迁移 409）；**一个收口点自动创建**（`handoff_service.ensure_ticket` 建单后开/并 Case——五类转人工触发零改动 + save_turn CSAT≤2 → SERVICE_QUALITY，Case 失败 SAVEPOINT 隔离绝不打断主链路）；**幂等合并**（同客户同类型一个活跃 Case，部分唯一索引 handoff 先例；并入追加 sessions/tickets/orders 关联，更高优先级提级重算 SLA）；**SLA 治理**（HIGH 4h/NORMAL 24h/LOW 72h 可配；cron `case_sla_check` 超时→ESCALATED+HIGH 幂等升级+即将超时指标，deploy/scheduler 第 4 任务）；坐席 API `/api/cases`（admin scope：队列/详情/认领/流转/resolve 必填 resolution_code/close/reopen 计数+SLA 重算/补偿评估）；**Service Recovery 并入**（`configs/compensation_policies.json` 纯政策表判定+reason_codes 留痕+评估快照进 metadata——**LLM 不参与补偿判定**，v1 只判资格不发放：发放是写操作随真实优惠券系统走确认门）；`service_cases_total{event}` 指标。遗留：补偿发放执行流、控制台 Case 页、超时坐席推送、部门指派。tests/stage34 11 项
- **Stage 35 身份核验分级（IAL）** —— ✅ 已实现（2026-08-05，需求 `docs/requirements/stage-35-identity-assurance/`——roadmap 3.7 第二项）：查订单/改地址不能只凭「知道订单号」。**等级声明进工具目录**（`ToolMeta.min_ial`，readonly 先例第二次应用：政策查询 IAL0/涉个人数据读写 IAL1/**改地址 IAL2**——盗号攻击面，未收录工具保守 IAL1）；**推导**（`app/core/identity.py` contextvar，budget 先例）：开发默认 `IDENTITY_DEFAULT_LEVEL=2` 沙盒互信=零回归，鉴权是**加成取 max 不降级**，生产下调基线后 IAL2 工具未达标结构性拒绝；**执法点两处模型绕不过**：ActionExecutor（写，`IDENTITY_REQUIRED` 拦在防重放**之前**不消耗执行权）+ tool_invoke（读，不调用工具）——均回核实话术+建单 `IDENTITY_VERIFY`（→ Case POLICY_REVIEW）。红线：OTP 类凭证未来接入不入日志；坐席走 admin API 不经聊天工具链。遗留：OTP 渠道（IAL2 可达性）、订单归属校验（需真实属主字段）、IAL3 工具。tests/stage35 6 项
- **Stage 36 事件驱动主动客服** —— ✅ 已实现（2026-08-05，需求 `docs/requirements/stage-36-event-driven/`——roadmap 3.7 第三项）：范式补全「业务事件发生→系统判断是否联系客户」（NBA 基建另一半）。`POST /api/events`（admin webhook）→ `event_service`：事件白名单（SHIPMENT_DELAYED/REFUND_STATUS_CHANGED/BACK_IN_STOCK/COUPON_EXPIRING，白名单外拒收）→ **幂等**（event_id SETNX 7 天同一事件只通知一次，Redis 故障宁可不发）→ **退订对服务通知同样生效**（复用 proactive optout/cool 键——用户「别打扰」优先于分类）→ 静默时间（`EVENTS_QUIET_HOURS` 跨零点支持）→ 渠道定位（最近会话，无渠道如实记录不硬发）→ **发送前重查最新事实**（verify_tool 经工具层，失败 fail-closed 不发——旧事件绝不带过期事实发出，测试锁定「通知内容来自重查非事件旧值」）→ i18n 模板消息落会话（metadata 带 event_id/模板=**发送依据可追溯**）+ WS 实时推送（Stage 15 通道复用）。`proactive_events_total{event_type,outcome}` 指标全结局可观测。**默认 `EVENTS_ENABLED=false` 零回归**。遗留：Outbox 投递保证、多渠道下发、事件驱动 Playbook、静默级差。tests/stage36 8 项
- **Stage 37 知识缺口发现 + 规则版质量 Scorecard** —— ✅ 已实现（2026-08-05，需求 `docs/requirements/stage-37-kb-gaps-scorecard/`——roadmap 3.7 第四项，决策日志既有字段零埋点）：**缺口发现** `scripts/export_kb_gaps.py`——三类信号（UNKNOWN+FALLBACK 澄清兜底/RAG 拒答转澄清 retrieval_json.clarify/同问法高频重复）→ **关键词重叠并桶聚类**（交集 ≥2 或达小集合半数——分词边界不稳仍聚上，测试锁定；语义聚类留真实流量）→ 缺口报表 CSV（topic/样本数/失败模式/示例问句）+ `--create-drafts` 生成 faq_entry 草稿进 Stage 16 审核流（**答案不用 LLM 生成**——缺口本来就是系统不知道的知识，必须人工供给）；**Scorecard** `scripts/score_sessions.py`——逐会话规则评分（resolution -30/UNKNOWN 轮 -8 封顶 24/纠偏轮 -6 封顶 18/营销超 1 次 -5/低分差评 -20，纯函数测试锁定），**三标签并存**（auto_score/human_score 留空待人工/user_score=CSAT——自动分不作唯一真值红线）。遗留：LLM 会话级评分（真实 Key）、embedding 语义聚类、quality_daily 加质量分列。tests/stage37 8 项。**至此 roadmap 3.7 立项四阶段（34-37）全部落地**
- **Stage 38 客户旅程（Customer Journey）** —— ✅ 已实现（2026-08-06，需求 `docs/requirements/stage-38-customer-journey/`——skill pack 八能力最后一块归位）：**跨会话客户阶段**（NEW→REGISTERED→DISCOVERING→CONSIDERING→READY_TO_BUY→PURCHASED 单调不倒退 + at_risk 叠加标记非阶段——「已购客户在闹退款」两者共存）；`customer_journey` 表（migration `5c31f6db723b` 可回滚实测，tenant+user 唯一，signals_json 转移史封顶 20）；**全规则推导不用 LLM 猜阶段**（`derive_transition` 纯函数：强证据跳阶——注册 CONFIRMED/ORDER.CREATE/订单物流售后类=已购；弱证据（商品咨询/询价/对比）一次只推进一格防抖；CSAT≤2/投诉/退款 → at_risk、CSAT≥4 解除）；更新收口 save_turn（SAVEPOINT fail-open，先于 NBA 决策——本轮阶段立即可用）；**消费点**：活动配置 `eligible_journey_stages` 门控（声明了而阶段未知/不符不推——营销保守方向，包 campaign_example 字段照收）+ NBA reason_codes 落 `journey:<stage>` + `GET /api/observe/journeys/{user_id}`；`journey_transitions_total{to_stage}` 指标。红线：**旅程不驱动写操作**（只影响主动建议相关性）、AT_RISK 是分析口径不是新营销开关。遗留：REPEAT/AFTER_SALES 细分、流失挽回 Playbook 消费 at_risk（前置已就位）、移交包附旅程。tests/stage38 7 项
- **Stage 39 预约与资源调度** —— ✅ 已实现（2026-08-06，需求 `docs/requirements/stage-39-appointment/`）：客服终点常是**协调真实资源**（安装/维修/取件/退货上门/回访/演示）。**APPOINTMENT 域**（第 11 域）：`APPOINTMENT.BOOK`（L2 写，slots=service_type 词表/appointment_time 时间正则/phone，一次性表达「预约明天下午的空调安装，电话 138…」同轮抽满）+ `APPOINTMENT.CANCEL`（appointment_no AP 前缀正则）——规则层确定性触发（**CANCEL 先判**「取消预约」含「预约」；被动式「预约被取消了」不触发；取消订单语义零影响，测试锁定）+ 二判目录兜底零重训；确认门→ActionExecutor→mock 资源池（`create_appointment`/`cancel_appointment`：**七风险点 v1 处理**——容量 3 原子扣减满员 SLOT_FULL 走执行失败转人工/同 (phone,类型,时间) 幂等同号/过期 SLOT_EXPIRED 拒绝/取消释放槽位/时区回执明示北京时间（真实系统契约 ISO8601 带时区）/提醒与未到场走 **Stage 36 事件通道**（APPOINTMENT_REMINDER/MISSED 模板，幂等/退订/静默全继承=提醒频控）/改期 v1=取消+重约）；`query_appointment_slots` 读工具进诊断白名单（IAL1）；旅程强证据（约安装=已购→PURCHASED）。零新机制（复用盘点见需求第 3 节）。遗留：会话式时间槽浏览（任务中途工具机制，与验证码同缺口第三次出现）、改期原子接口、真实调度系统对接。tests/stage39 25 项
- **Stage 40 行为准则层（Behavior Guidelines，借鉴 Parlant）** —— ✅ 已实现（2026-08-06，需求 `docs/requirements/stage-40-guidelines/`，评估报告 `docs/architecture/parlant_evaluation.md`——**不引入 Parlant 框架**，借鉴其 condition-action 每轮动态注入思想）：定位一句话**「护栏管绝不允许（拦截），准则层管应该怎样（引导）」**。`app/chat/guidelines/engine.py`：准则表 `configs/guidelines.json`（campaigns 模式：mtime 缓存/example 8 条种子入库/损坏 fail-open 空表；字段 id/condition{intents 支持域前缀,states,emotion,keywords,tenants}/action/criticality/exclusion_group）；**v1 全规则匹配零 LLM 成本**（维度 AND×取值 OR×空维度不限）→ criticality 降序 + 同 exclusion_group 去重 + `GUIDELINES_MAX_INJECT=3` 条数封顶（防重蹈系统提示词膨胀的硬闸）；**只注入 LLM 增强路径**（润色/RAG 生成/智能澄清 system prompt 追加，确定性模板路径不经过；无 Key 注入点本不存在=天然无效）；命中 id 经 turn 级 contextvar 收集器 → save_turn 落 `graph_trace_json.guidelines`（零 GraphState/节点契约改动）+ `guidelines_injected_total{criticality}` 指标；**多租户准则表（condition.tenants）是对 Parlant 的差异化**。默认 `GUIDELINES_ENABLED=false` 零回归。遗留：embedding/LLM 匹配器（进 Stage 17 预算）、坐席草稿注入、命中率/违反率看板、效果验证待真实 Key。tests/stage40 8 项
- **生产就绪审计** —— ✅ 已落地（2026-08-05，五问审计：并发乱序/离线预测线上/外部写幂等/复杂度/可回滚）：清单 `docs/ops/production_readiness.md`（活文档：✅已具备/🔧本次补齐/⏳触发时机+验收动作，末节上线前必过红线清单）。**本次补齐三件**：①端到端幂等契约——ActionExecutor 写操作 params 携带 `idempotency_key=task_id`，对接契约（服务端按键去重/≥7天/审计对账）写入 stage-11 文档第 6 节；②DB 迁移回滚演练——临时库实测全部 22 个迁移单步与**全链 downgrade→重升均可执行**（注：空库结构可逆，带数据降级先备份）；③状态结构回滚容错回归锁——task/栈未知字段透传不丢、缺可选字段宽容降级（tests/stage29）。全量 460 passed
- **Post-Stage 20 全链路 Review 加固** —— ✅ 已实现（2026-07-27，记录见 `docs/requirements/post-stage-20-review-hardening/`）：六批整改零 migration 零协议变更，274→300 tests。**意图控制层**（纯放弃判定防长句误吞、身份先于转人工、取消正则排除被动式，评估集固化）；**正确性**（语义缓存个性化回答不进共享缓存防跨用户泄漏、metadata_json JSONB 合并+CAS 防并发覆盖、向量召回生效过滤提前到 rerank 前、摘要滚动步长 `MEMORY_SUMMARY_STEP`）；**容量**（阶段边界 commit——LLM 中段连接池占用为 0、轮级 LLM 时间预算 `TURN_LLM_BUDGET_SECONDS` 耗尽走降级、记忆任务 Semaphore `MEMORY_TASK_CONCURRENCY`）；**延迟**（embedding 一轮 4→1 次、语义缓存 numpy+to_thread+pipeline、RAG 生成收口 factory `purpose="rag"` 补回指标/trace/预算、向量∥关键词并行召回、章节上下文批量化、`kb_stage_duration_seconds` 分段指标、jieba/重排预热）；**演进**（blocked 条件边短路透传节点、多意图段并行分类、trace.reranked 记实际行为、歧义检测向量分同源、A/B 轮绕过语义缓存、Milvus 客户端加锁+探活复用）；**RAG 强化（WeKnora 对齐）**（含糊查询 LLM 改写先重写再检索——改写轮绕过缓存、引用溯源 `[n]` 解析到 chunk 粒度落 trace.cited、生成上下文跨块段落清洗、关键词 BM25-lite 细排）。遗留：真流式 SSE / LLM 理解调用合并 / 语义缓存迁 Milvus 需真实 LLM/Milvus 环境
- **Stage 20 记忆摘要 v2** —— ✅ 已实现（2026-07-21）：结构化会话摘要（`memory_summary` 改固定 schema JSON：诉求/entities 具体单号/进展/待办/已答复，「保留具体值禁止概括」prompt 硬约束；数组各限 5 条丢最旧、序列化超 500 字拒写保留旧摘要；解析失败降级纯文本存储，存量旧格式兼容；渲染为紧凑字符串注入——`MemoryContext.session_summary` 对外仍是 str，下游零改动）；单一表示不变式固化（摘要覆盖区间与短期窗口互斥，`tests/stage20` 参数化回归锁定 + 增量续写不重复纳入）；摘要增量经 `wrap_user_input` 防注入补齐；MCP 大工具结果对接红线写入 stage-11 文档第 5 节（doc-only：服务端返回结论字段，原始大 JSON 不进 prompt）。**明确不建** L1/L2/L4 长链路上下文防线（本链路无此膨胀模式）、不动 mem0、不加 migration。全量 273 passed 零回归（2 个失败为 Milvus 未启动环境问题，基线同样失败）

## 目录结构

```text
app/
  main.py              # FastAPI 入口、lifespan 资源管理（Redis/DB 探活/WS 枢纽/Langfuse flush）、路由注册
  api/routes/          # 接入层，只接参数/调用 service/返回响应
    health.py          # GET /api/health、/api/health/ready
    chat.py            # 发消息 / SSE 流式 / 会话创建 / 历史分页 / feedback / notify（主动消息）
    handoff.py         # 坐席工单 API：队列/详情/claim/reply/resolve（admin scope，Stage 07）
    ws.py              # WS 双端实时通道（用户端 + 坐席端，Stage 15）
    kb.py / product.py # 知识库 / 商品管理面（admin scope）
    metrics.py         # GET /metrics（Prometheus，Stage 09）
  schemas/chat.py      # Chat API 请求/响应 Pydantic 模型
  services/
    chat_service.py    # 应用服务层：协调 Repository + LangGraph 图 + 指标/追踪/记忆/反馈
    handoff_service.py # 转人工闭环：幂等建单/上下文移交包/坐席操作/CSAT 询问（Stage 07/15）
    notify_service.py  # WsHub：WS 连接枢纽 + Redis Pub/Sub 跨进程广播（Stage 15）
  core/                # config(生产硬门禁) / logging / exceptions / responses / auth(API Key+吊销)
                       # rate_limit / idempotency(在途锁+指纹) / metrics(Prometheus) / tracing(Langfuse)
  db/                  # base.py(Declarative Base + Mixin，末尾导入 models 供 autogenerate)、session.py(async engine/池)
  cache/redis_client.py# 全局 async Redis client，startup/shutdown 管理
  models/              # ORM：chat_session/message/dialog_state/decision_log/task/tool_call/
                       # handoff_ticket/feedback/csat/api_credential/kb_*/faq_entry/product_item/user_memory
  repositories/        # 数据访问层，BaseRepository + 各表 repository（只做 CRUD，不写业务）
  chat/                # 聊天主链路
    graph/             # LangGraph：state.py / builder.py / nodes/（8 线性节点 + 5 回复分支，见 system_overview 第 4 节）
    guardrail/         # 内容安全护栏：lexicon(规则库解析) + engine(注入/违禁/灌注/输出检查，Stage 14)
    tools/             # ToolProvider 协议 + mock / MCP 客户端（TOOL_MCP_FALLBACK 降级策略，Stage 05/11/13）
    actions/           # ActionExecutor（唯一写入口，原子拿执行权防重放，Stage 05/13）
    agents/            # 只读诊断 agent：READONLY_TOOLS 白名单 + 结构性终止循环（Stage 22，默认关）
    confirmation/      # 确认门含糊应答 LLM 解析（Stage 05；L3 弱确认收紧在 confirmation_parse 节点）
    intent/            # 混合分类器：规则控制层 + SetFit + LLM 二判 + 多意图切分（Stage 03/04/10）
    mode/              # 对话模式门：闲聊/业务/混合/OOS 四模式，共享 SetFit body（Stage 30，默认关）
    proactive/         # 主动服务：NBA 规则策略 + 活动池（抑制矩阵/频控/拒绝冷却，Stage 31，默认关）
    slots/             # SlotExtractor + 正则 patterns + LLM 兜底
    state/             # DialogStateManager 状态机
    skills/            # SkillRegistry 能力声明 + responder 模板回复 + llm_responder 润色
    memory/            # MemoryProvider 协议：local(窗口/摘要/长期事实) + mem0（Stage 10）
    cache/             # 语义缓存：SemanticCacheProvider 协议 + Redis 实现（同问直答，Stage 17）
    logging/           # DecisionLogger 决策日志落库（落库前脱敏）
    llm/               # factory(统一收口+timeout+降级+分级路由) / budget(租户预算熔断) / prompts / prompt_guard(防注入包裹，Stage 14/17)
    csat.py            # CSAT 评分解析（Stage 15）
  kb/                  # Stage 06 知识库：chunker/embedding/ingest/retriever/answerer/reindex/query_normalize
    backends/          # VectorStoreBackend 协议 + Milvus 实现 + factory
    parsing/           # 文档解析：Block IR + MinerU(HTTP)/Docling/内置解析器 + 格式路由
  product/             # 商品库：ProductProvider 协议 + 本地实现（价格/库存唯一事实源）
  experiments/         # A/B 实验：config(JSON 加载) + resolver(确定性分桶+白名单参数注入，Stage 18)
alembic/               # 异步迁移，env.py 从 settings 读取 DATABASE_URL
scripts/               # 训练/密钥/DDL 导出/回流导出/回放/看板刷新/生命周期/MCP 服务
tests/                 # 按 stage 分目录 + eval 评估门禁（意图/多意图/RAG）
web/                   # Web 测试控制台（Vue3+Vite4+Element Plus，Stage 28）：登录/多级菜单/对话控制台
docs/                  # 需求、架构、API、数据库、测试、运维文档（按阶段拆分）
skills/                # Skill 声明（33 个 md + README 落点对照，YAML front-matter；Loader 启动加载——运行时配置；与注册表双向覆盖测试锁定）
configs/               # 运行时 JSON 配置（campaigns.example.json 活动池示例等）
sql/ddl/               # 建表 SQL（scripts/export_table_ddl.py 生成物，勿手改）
```

## 常用命令

```bash
uv sync                                      # 安装依赖
uv run uvicorn app.main:app --reload         # 启动服务
curl http://localhost:8000/api/health        # 健康检查
uv run alembic revision --autogenerate -m "msg"  # 生成迁移
uv run alembic upgrade head                  # 执行迁移
uv run ruff check app alembic                # 代码检查
uv run mypy app                              # 类型检查
uv run pytest                                # 测试
uv run python scripts/build_intent_dataset.py   # 构建意图训练集 v42（v41→taxonomy 映射+FAQ 生成）
uv run python scripts/train_setfit_intent.py    # 训练 SetFit 意图分类器（产物 models/intent_setfit_v1）
uv run python scripts/train_meta_classifier.py  # 训练 Meta-classifier 会话决策器（先 uv sync --group train；产物 models/meta_classifier_v1）
uv run python scripts/export_meta_training_set.py --tenant t1  # 导出线上真实特征表（影子采集→训练契约 CSV，改标后重训）
uv run python scripts/build_intent_example_index.py   # 构建意图示例向量索引（KNN 交叉验证用；SetFit 重训后必须重建）
uv run python scripts/train_mode_gate.py        # 训练对话模式门四分类头（共享 SetFit body；SetFit 重训后必须重训）
uv run python -m app.kb.reindex --tenant t1     # 从 PG 重建 Milvus 向量索引
uv run python scripts/export_table_ddl.py       # 从模型导出建表 SQL（sql/ddl/，生成物勿手改）
uv run python scripts/refresh_quality_views.py  # 刷新质量看板物化视图（quality_daily）
uv run python scripts/export_review_set.py --tenant t1           # 回流待审样本导出（--mode faq 为 FAQ 沉淀）
uv run python scripts/replay_trace.py --trace <id> | --session <id>  # 决策回放
docker compose up -d                            # 起 PG+Redis（--profile kb 加 Milvus）
cd web && npm install && npm run dev            # Web 测试控制台（:5173，/api 代理到 :8000，Stage 28）
```

> 本地依赖 PostgreSQL 与 Redis（必需）、Milvus 2.5+（Stage 06 知识库，可用 `KB_ENABLED=false` 关闭），启动方式见 `docs/ops/local_dev_and_runbook.md`。配置复制 `.env.example` 为 `.env`，`DATABASE_URL` 必须用 `postgresql+asyncpg://` 前缀。开发模式 `EMBEDDING_PROVIDER=hash` 时需降低检索阈值（FAQ_HIT_THRESHOLD=0.6、RAG_MIN_SCORE=0.2）。知识库索引重建：`uv run python -m app.kb.reindex --tenant <id>`。

## 代码约定（强约束）

- 核心类、核心方法、复杂逻辑必须有**中文注释**。
- route 只做接入，业务逻辑放 Service / 决策层，不要堆进 route。
- 数据库地址、Redis 地址、API Key **禁止写死**，统一走 `app/core/config.py` 的 `settings`。
- 日志**禁止打印**明文 API Key、手机号、地址、订单等敏感信息。
- 所有外部访问（DB / Redis / LLM）必须有 **timeout**；连接资源必须支持**优雅关闭**。
- 所有异常必须记录日志并返回**统一响应结构**（`app/core/responses.py`）。
- DB engine / Redis client 全局复用，**不要每次请求新建**。
- 写操作必须预留**确认门**，禁止让 LLM 直接执行退款 / 取消 / 改地址等。
- 严格按阶段需求文档执行，**不跨阶段**擅自实现 RAG / FAQ / 向量库 / 真实工具。

## 技术栈约束

Python 3.12（uv pin）、uv 包管理、SQLAlchemy 2.x async + asyncpg、Alembic、redis.asyncio、Pydantic v2 + pydantic-settings。
